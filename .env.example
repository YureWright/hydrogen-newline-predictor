# 高德开放平台 Web 服务 Key（https://lbs.amap.com 注册，免费）
# 申请时服务平台选「Web服务」，并勾选服务权限：
#   驾车路线规划（必选）/ 地理编码（推荐）/ 输入提示（推荐）/ 交通态势（可选）
# 配置后：复制本文件为 .env 并填入你的 Key（.env 已被 gitignore，不会提交）
AMAP_KEY=your_amap_web_service_key

# ===== AI 路线评估（DeepSeek，OpenAI 兼容接口）=====
# 必填：AI 评估功能需要
DEEPSEEK_API_KEY=your_deepseek_api_key
# 可选：自定义端点（默认 https://api.deepseek.com）
DEEPSEEK_BASE_URL=https://api.deepseek.com
# 可选：模型名（默认 deepseek-v4-flash）
DEEPSEEK_MODEL=deepseek-v4-flash


# ===== 天气数据（可选，按出发时间+位置匹配沿线温度/风速/湿度/降水） =====
# 推荐主源：和风天气 QWeather（免费 1000 次/天，逐小时 24h，中国大陆 GCJ-02 坐标与高德对齐）
# 注册：https://dev.qweather.com 控制台 → 项目管理 → 创建项目 → 复制 Key
QWEATHER_KEY=
# ⚠️ 必需：和风天气 Console V4 起每个账号有专属 API Host（旧共享域名 devapi.qweather.com 2026 年起停用，不配会报 403 Invalid Host）
# 获取：控制台 → 设置 → API Host，形如 abc1234xyz.def.qweatherapi.com（不要带 https://）
QWEATHER_HOST=
# 兜底：高德天气（复用上面 AMAP_KEY，无需新 key；免费 30 万次/天，但只有实时+4天日预报，时间匹配较粗）
# 可选第三源：OpenWeatherMap（免费 1000 次/天，48h 逐小时；WGS-84 坐标，需内部转换）
# 注册：https://openweathermap.org/api → One Call API 3.0 → 复制 API Key
OPENWEATHER_KEY=
# 风速阈值 km/h（默认 10.8≈3m/s）：超过才算"大风"计入风阻，可在代码/前端配置
WIND_THRESHOLD_KMH=10.8
