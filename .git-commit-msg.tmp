fix: 道路切分全面审查——同事件区合并成单段 + 事件段典型均速 + 语义统一

审查发现并修复 3 个问题：

1. [用户报告] 同一收费广场被切成"进入收费站+驶出收费站"等多个 step 时，
   只第一个挂变速事件，后续 step 显示"收费站但无变速"——空壳段+展示不一致。
   → buildSegments 改为直接把连续同类事件 step（收费站/匝道/服务区）合并成一个段，
     事件只保留一份，每座收费站/每处匝道 = 1 个带变速事件的段。

2. [物理重要] 长 step 拆分出的尾部事件段均速继承了整步 90+km/h 的高速均速，
   而实际过收费站/匝道只有 20~40km/h——单次停车动能 0.5mv² 会按 93km/h 算，能量差约 5 倍。
   → 新增 EVENT_SPEED_KMH（收费站25/匝道35/转弯30/服务区30/路口25），尾部事件段用它并重算时长。

3. [语义统一] 事件段判断抽出 isEventBehavior() 单一事实源，shouldSplitByGrade 复用；
   事件段不参与地形切分、expectedStopCount 无事件事件段计 0 等语义保持一致。

实测乌兰察布→天津 490km：收费站 2 座均带变速（0.1）、匝道 5 处均带变速（0.05）
verify:split 40 项 + verify:segment + verify:route + tsc + build 全过