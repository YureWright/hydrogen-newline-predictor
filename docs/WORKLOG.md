# 工作日志（WORKLOG）

> 记录项目关键里程碑与每日工作内容，与 git 提交历史一一对应（`git log --format='%ad %h %s' --date=short`）。
> 日期按"当日最后提交"归组；更细粒度请直接看 git log。

---

## 2026-08-23 · 物理模型加入「启停能耗」（L2b，启停按期望次数计入）

| 工作 | 说明 |
| --- | --- |
| 触发 | 用户连续拷问 SimLab 与预测物理模型"算不算启动/停车"→ 确认此前纯匀速巡航（F_acc=0、t=s/v），要求按最高标准补上并同步文档 |
| 模型 | 每段按期望停车次数 N 计入两类能量：① 动能净损耗 E_stop^ke = N·½δmv²·(1/η_mt − η_regen·η_mt)（加速注入 ½δmv²/η_mt、制动回收 ½δmv²·η_regen·η_mt）；② 停车附件/怠速 E_stop^idle = P_fc^stop·N·t_stop（P_fc^stop=max(P_fc^min, P_aux(T))，电堆最低稳定运行）；E_fc = P_fc·t + E_stop^ke + E_stop^idle |
| 参数 | η_regen=0.30（低速城市启停回收低、机械制动为主；区别于设计文档 §4.4 高速下坡回收 0.6~0.7）；单次停车时长按行为类型：收费站 60s / 服务区 40s / 红绿灯 30s / 匝道 20s / 转弯 10s，默认 30s |
| 输入链路 | 前端 slim payload 新增 `stopCount`（expectedStopCount 权威口径）+ `stopSecondsPer`（按段主导行为类型估计）→ /api/predict-hydrogen → ml/physics.py |
| 回归保证 | **stopCount=0 时 E_stop≡0**：手算工作簿三路段（附录 B）合计 2.040 kg / 8.50 kg/100km 逐位不变（已实测）；新增中间变量 6 个（N_stops/t_stop_total_h/E_stop_ke_kwh/E_stop_idle_kwh/E_stop_kwh/m_H2_stop），物理表 19→25 列 |
| SimLab | physics-lab.html 同步同公式 + 「停车次数」滑杆（0~20）+ 高级参数「单次停车时长/再生回收比例」，与后端逐公式一致；ML 对比请求也带上启停输入 |
| 文档同步 | 技术原理 §7.1（输入表+N）/§7.2（L2b 公式，重编号 ①②③④）/§7.4（启停示例 5km 城市 6 次停车 → +0.254kg，高约 88%）；README（21 个中间变量/启停行/手算说明/25 列，修正"暂忽略 σ"旧表述）；设计文档 §4.5 新增 + 伪代码⑥注释 + 附录 B 说明；WORKLOG 本条 |
| 验证 | physics.py 回归（N=0 → 2.040kg 不变）+ 带启停用例数值手算复核一致；tsc --noEmit + vite build 通过 |



| 工作 | 说明 |
| --- | --- |
| 审查结论 | 物理修复方向正确：F_roll·cos(θ)、F_aero 加 σ²（E[v²]=v̄²+σ²）、再生 P_drive 改 P_wheel×η_mt（旧代码 /η 使回收电量虚高 ~20%）、P_aux 上下限可 override；前端重复 hybrid 下拉（我上轮 Replace 重叠所致）已删、表格提示/依赖数组/坡度缺失警告均合理 |
| 发现的文档漂移 | PR 只改了代码没同步文档：设计文档附录B、技术原理 §7.2/§7.4/§10、README 的下坡中间变量仍是旧口径（P_drive −55.3→−44.1、P_bat −85.3→−74.1、合计 2.039→2.040）——已全部同步 |
| 备注 | "对照设计文档 §5"表述略虚：设计文档 §5.1 只写了驱动公式，附录B 原来就把 /η 误用到再生段；修复本身物理正确（机械→电链路损耗应收敛），但文档自洽性靠本次同步补齐 |
| 验证 | tsc/vite build 通过；E2E：Landing→预测→物理模型 7.31 kg/100km（PR#10 后复测）、坡度缺失警告（2 段）生效、hybrid 占位保留 |

## 2026-08-23 · 物理+数据驱动融合模型：文献调研 + 初步设计（待开发）


| 工作 | 说明 |
| --- | --- |
| 触发 | 用户想加"人工神经网络"，隐藏层要有可解释含义：输入（天气/路况）→ 车辆内部物理量 → 氢耗 |
| 关键澄清 | 原想法"电流降维当隐藏层"是死路（电堆/电机电流是氢耗的"果"，新路线拿不到）；改为"预测内部物理量当隐藏层"（输入全可抓取） |
| 文献调研 | 核心锚点：Lei et al. 2025 TCNN（IEEE TVT 74(6):8907-8920，理论约束+模块可解释，FCET 能耗，氢耗精度相对传统效率曲线法 +63.7%）；灰盒框架（物理管骨架/数据管部件）、2023 Energy 残差校正、EV-PINN、库内 Kamal2021（白盒骨架+黑盒零件） |
| 数据实测 | 车辆1 清洗后 1055 行×80 列；72 数值列 PCA 前 10 主成分解释 88.7% 方差；与氢耗相关性最高的全是电流类信号（|r|≈0.565）——印证"电流不能进输入" |
| 产出 | docs/物理数据驱动融合模型_初步设计.md（架构/双监督源/两条落地路线/红线/路线图/参考文献）；文档库注册；前端预测模型下拉加「🧪 物理+数据驱动混合（待开发）」占位；技术原理 §11 待开发说明 |
| 状态 | 🔜 设计完成、待开发（P1 实车功率回填 → P2 残差校正MLP → P3 完整可解释隐藏层NN → P4 前端接入） |

## 2026-08-23 · 车型选择（海珀特全系预设 + 参数可调）


| 工作 | 说明 |
| --- | --- |
| 触发 | 用户要求"预设海珀特所有车型作为输入，可直接选择、也可调整相关参数" |
| 车型调研 | 官网 hybot.com.cn：H49 49t 牵引车（300kW 燃料电池 / 75kWh 电池 / Cd=0.35 / 满载 7.1kg·100km⁻¹）；H18 18t 厢式运输车（120kW / 50kWh / 整备<9.8t / <5.5kg·100km⁻¹）；H4.5 4.5t 冷藏车（80/90kW / 14.9kWh / 整备 3.7t / <3.10kg·100km⁻¹） |
| 实现 | ① `ml/physics.py`：predict_segment 从每段读取 `crr/cd/frontArea/eta_mt/p_fc_min/p_fc_max/p_bat_max/eta_fc/p_aux0/k_t`，未传回落 H49 默认；响应回显 `vehicle`（本次实际所用参数）② 前端「🚛 车型选择」三款预设一键切换 + 「高级参数」折叠面板可调 11 项物理参数 + 整备质量；载重上限按所选车型 GVW−整备自动收窄 ③ 结果区动态显示所选车型与参数 |
| 诚实披露 | H18/H4.5 的 Cd 与迎风面积官网未公布，按同族厢式/冷藏车常规值估算（H18 由外廓 2.54×3.95m 推导），界面与文档均标注"*估算，可手动修正"；ML 模型仍用 H49 实车数据训练，切其他车型仅质量特征联动，界面提示以物理模型/双引擎为准 |
| 验证 | `tsc --noEmit` 通过；physics.py 默认与自定义参数两组段数据端到端计算正常，`vehicle` 回显正确 |

## 2026-08-23 · 校准评估与移除


| 工作 | 说明 |
| --- | --- |
| 触发 | 用户问"校准公式有没有在训练数据上测过、R² 有没有提升" |
| 实测 | 训练数据 CV 对比：无校准 R²=0.4780/RMSE=0.0477；中位偏差校准（曾用）0.4765/0.0478（略降）；均值偏差校准 0.4793/0.0476（最优但提升微乎其微） |
| 根因 | 模型均值意义已准（各均速段均值偏差 +0.03~0.37 kg/100km）；校准减的是中位偏差（0-40 段 +1.89），偏离 MSE 最优 |
| 处理 | 移除 predict.py 的中位偏差校准，预测直接用模型原始输出；技术原理 §6.4 改为"校准评估与移除" |
| R² 口径澄清 | 0.38=每折 R² 取平均（保守口径，meta/本页用）；0.48=整体 OOF R²（同一批预测拼一起算）——两种算法不可直接比较，已在文档注明 |

## 2026-08-22 · 多模型对比 + Stacking 评估

| 工作 | 说明 |
| --- | --- |
| 触发 | 用户提出：特征换成天气/道路工况数据后 VIF 可能低，HistGB 是否仍最优？要不要 stack |
| VIF 实测 | 20 特征平均 VIF=226、最大 2106（v_mean/v_p85 强相关）——共线性仍高，与"会变低"的预期相反 |
| 模型对比 | `ml/compare_models.py`（与 train 同管道、按行程分组 5 折 CV）：HistGB 0.382 最优 > RF 0.370 > GBR 0.336 > 线性 0.25-0.26 > SVR/MLP；Stacking(hist+gbr+ridge) 0.380 无提升 |
| 结论 | HistGB 确为最优单模型（VIF 高时树模型免疫共线性）；Stacking 无提升且复杂，不采用；train.py 保持 HistGB |
| 踩坑 | 首次 compare 脚本多了 `len(s)<2` 过滤导致段数/管道差异，误报"Stacking 提升到 0.45"——修正管道一致后证伪 |

## 2026-08-22 · 训练数据质量审计（h2_remain 毛刺）

| 工作 | 说明 |
| --- | --- |
| 触发 | 天津→乌兰察布预测低至 2kg/100km，与官方满载 7.1 偏离 → 审计目标变量来源 |
| 能量对账（金标准） | 电机电能 ÷ 氢气能量（物理上应 <0.5）。**电机功率换算已验证正确**（停车=0、巡航 68~82kW 匹配 20~40t） |
| 发现 1 | 原始 h2_remain 毛刺占比 >99.9%（虚假下降 73686kg vs 真实 65kg，800 倍）——**整列不可靠** |
| 发现 2 | 用户清洗算法合理，但清洗后目标仍系统性低估 ~2 倍（车1 65kg vs 理论 130kg）——毛刺 800 倍背景下算法极限 |
| 发现 3 | 累计氢耗列有大量清零重置、且与 h2_remain 同源，不能独立校准 |
| 结论 | 模型链路完整，预测偏低源于企业原始数据；建议企业提供可靠储氢计量。详见 docs/data-quality-audit.md |

## 2026-08-22 · 新增爬升密度特征 gain_m_per_km

| 工作 | 说明 |
| --- | --- |
| 背景 | 用户追问"特征全不全"——盘点发现：当前只用平均坡度，合成工况坡度还是恒定值，段内起伏（爬升+下降）完全丢失；实测爬升密度与氢耗 r=+0.130 有增量 |
| 实现 | 训练聚合（train/calib_check/report）从 elev 序列算段内正海拔差和÷里程 → gain_m_per_km；feat.py ACQUIRABLE 加特征（特征总数 19→20）；predict.py 从前端 gainM（elevationGainM）÷L 得密度；前端 slim 传 gainM |
| 重训 | 里程加权版 R² 0.364 → **0.382**、RMSE 0.0479→**0.0473**（历史最低）；gain 相关性 r=+0.138、重要性 0.035（排第 9）。验证了"爬升密度补上平均坡度丢失的起伏"的价值 |

## 2026-08-22 · 里程加权聚合（用户审查聚合口径后）

| 工作 | 说明 |
| --- | --- |
| 聚合口径审查 | 用户指出：① 段内 v/grade/elev/temp/wind/hum 用算术平均会因"低速点更密"而灌水（实测均速最高低估 16%、坡度偏差更大）；② 道路等级取众数 + 序数编码喂树模型是标准做法（分类变量平均无意义、独热对树模型无益）；③ hour 保留（重要性 0.12 排第 3，数据支持）；④ 5km 聚合有实证（点级按行程分组 CV R²=0.16 → 段级 0.49） |
| 里程加权聚合 | train/calib_check/report 的 aggregate：v/grade/elev/temp/wind/hum 由算术平均改为**里程加权平均**（每点按它代表的里程加权，与预测端高德整段均速口径对齐）。重训：R² 0.384→**0.364**（CV 噪声内，±0.22）、RMSE 0.0481→**0.0479**；保留（口径更正确 + RMSE 微降） |
| 坡度切分决策 | **不做**：训练数据 60s 粒度粗（1km/点），无法复刻预测端 200m 采样+500m 滑窗的坡度切分；且无行为区（高德 step/收费站/匝道）维度；坡度变号频繁会切出 <1km 小段，目标 h2/km 噪声爆炸。保持 5km 固定段 |

## 2026-08-22 · 质量特征（行程级能量平衡）+ 前端载重曲线

| 工作 | 说明 |
| --- | --- |
| 质量反推（ml/mass_est.py） | 用户要求 ML 引入重量。训练数据无载重列，但有三驱动电机 V/I（官方换算：电流 −1000A、电压 ×0.1）。用**整趟行程能量守恒**反推质量：E_drive = m·g·Σ(sinθ+Crr·cosθ)vΔt + Σ½ρCdAv³Δt ⇒ m = (E_drive−E_aero)/[gΣ(sinθ+Crr·cosθ)vΔt]。动能项闭合消去；空调等附件走并联支路不经电机，不进方程。实测：车1 解出 3 趟（中位 32.0t）、车2 解出 14 趟（中位 36.1t，范围 11.3~55.5t） |
| 单点反推 vs 行程级 | 用户质疑"一路质量不该变"——验证单点反推行程内 CV=39.5%（噪声大，弃用）；行程级能量平衡每趟一个值，符合物理 |
| 能量闭合验证 | 附件 12-20kW（占电机 15-25%）独立于电机回路；电源(FC+电池)/负载(电机+附件)闭合比 0.70，暴露 CAN 换算系统偏差 → 用官方锚点（满载 49t）校准 |
| 特征接入 | feat.py ACQUIRABLE 加 mass_kg；train/calib_check/report 的 aggregate 按行程取质量；predict.py 支持每段 massKg（默认 30t） |
| 重训结果 | R² 0.378 → **0.384**（RMSE 0.0483→0.0481），提升微弱；但载重响应方向正确（同路段满载 49t 6.54、空载 10t 5.79 kg/100km，+13%）。**局限**：树模型对稀疏质量档位是离散响应，城市高载重出现不合理下降 → 可靠载重敏感性依赖物理模型（开发中） |
| 前端载重曲线 | 预测卡片新增「⚖️ 载重输入」：固定载重（t）或**重量曲线**（按里程关键点线性插值到每段，支持中途装卸货）；每段 massKg = 整备 9.7t + 插值载重 |
| 文档 | 技术原理 §2.5/§2.6（质量特征原理+公式）、§3（19 维）、§6.1/§6.2（R²/重要性）；README、ml/README 同步 |

## 2026-08-22 · PR #8 代码审查 + 训练距离修复重训

| 工作 | 说明 |
| --- | --- |
| PR #8 审查（其他 AI 提的 4 处修复，已 merge） | ① predict.py `or default` 把 0 当缺失（0°C/0 风速/海拔 0m/午夜 0 点被静默替换为默认值）——**确认合理**；② SegmentsPanel 氢耗预测后 setInterval 未清除（内存泄漏）——**确认合理**；③ 氢耗图表高耗标记 x 坐标先 filter 后累加导致偏左——**确认合理**；④ train/calib_check/report 的 haversine 向量化 bug——**确认真实存在**，见下 |
| 那个"距离"是什么 | **实车 GPS 轨迹相邻两个 60s 采样点之间的球面（haversine）距离**。训练时用它把轨迹按 5km 聚合分段，段里程 L 是训练目标「每公里氢耗 h2_per_km = h2used / L」的**分母**，也是 len_km / e_acc / e_aero / e_grade_up 特征的分母。它不是高德路线的段里程（预测输入仍用高德距离），只存在于训练/回填链路 |
| haversine bug 影响量化 | 原代码 `np.cos(p1[0])*np.cos(p1[1])` 固定用了轨迹前两个点的纬度，未逐点取 `cos(lat_i)*cos(lat_{i+1})`。实测真实数据逐点距离：车辆1 平均偏大 0.85%（最大 4.2%）、车辆2 平均偏小 0.73%（最大 5.5%）；5km 段聚合后部分抵消 |
| 重训（train.py） | 修复后 haversine 重训：**1066 段**（旧 1064），目标中位 5.12 kg/100km；**CV R²=0.378**（旧 0.342）、**RMSE=0.0483**（旧 0.0507）——目标变量更干净，指标反而提升；trained_at 改为动态日期（2026-08-22） |
| 校准重拟合 | 重训后无校准偏差表变化（0-40 +1.6→+0.76、40-60 +0.9→+0.96、60-80 +0.3→+0.09、80+ +0.5→+0.50、整体 +1.2%→+1.0%），predict.py 校准公式从 `clip(1.8-0.02v)` 更新为**分段常数**；技术原理页 6.1/6.2/6.4、README、ml/README 的 R²/RMSE/偏差表数字全部统一为最新值（不造假） |
| 验证 | tsc 通过；KaTeX cases 公式渲染正常；predict 冒烟通过（0 值保留、缺失兜底、不同均速校准生效） |

## 2026-08-21 · 氢耗预测收尾 + 物理模型设计

| 工作 | 说明 | 关键提交/文档 |
| --- | --- | --- |
| 物理模型设计方案 | 基于 `physics-model-research.md` 调研，完成《物理氢耗模型_设计方案.html》：五层能量流水线（路段能量矩积分 → 四阻力 → 动力总成 → 燃料电池效率 → 氢耗折算与三重锚点校准），提出 8 个创新点（坡段能量矩积分 / E[v²] 速度波动修正 / 等效摩擦角 / 再生制动+SOC 门限 / 温度-附件-电堆耦合 / 载重显式建模 / 简化 ECMS 的 SOC 平衡 / 双口径折算+锚点校准），KaTeX+SVG 图文讲解 | `docs/物理氢耗模型_设计方案.html`（b849d3c） |
| 氢耗明细表增强 | 固定高度 420px + 垂直滚动（表头 sticky）；点击表头排序：全部数值列 + 道路名 + `#`（路线顺序），升降序切换；null 沉底、中文道路名 localeCompare | d85d1c6 |
| 预测校准与兜底透明 | 低速段系统偏差校准（§6.4 写入技术原理页）；数据源状态条（天气/OSM/DEM 覆盖 + 兜底提示） | e1d7137 / cc7767b |
| 实车数据重训 | 用户修正 `h2_consum_per_sec`（即 h2_remain 差分，同源同单位）后重新回填+重训；数据字段换算对齐官方《数据计算方法》 | 899b534 / df345cb / ed04ecd |
| 技术原理页论文级升级 | 纯 JSX + KaTeX 本地化 + SVG 配图；训练步骤/模型选择/实测训练报告（相关性、特征重要性）全流程透明、不造假 | cab4b96 / 995cbea / 5a2d0c7 |

## 2026-08-20 · 视觉主题与体验

| 工作 | 说明 | 关键提交 |
| --- | --- | --- |
| 海珀特深空科技主题 | 全站深空暗色主题，头图启用官网整车主视觉；修正三处深色主题对比度不足 | 04d8665 / b196ee9 |
| DEM 下载体验 | 修"一卡一跳/像卡住"，加多阶段步骤指示器 | 470260f |
| OSM 认路修正 | 反向国道不再交给小区路、市区路不再被升成国道；修正五处静默算错氢耗的口径缺陷 | e0b4d8c / 6037ebe |
| QWeather 适配 | 适配 Console V4 专属 API Host + 最近小时匹配 | ec1e757 |

## 2026-08-19 · 天气与道路等级

| 工作 | 说明 | 关键提交 |
| --- | --- | --- |
| 沿线天气模块（A3） | 按"出发时间 + 位置 + 时刻"逐段匹配温度/风速风向/湿度/降水（QWeather 主源 + 高德兜底） | 449fe77 |
| OSM 真实路网道路等级 | Overpass 地图匹配替换规则推断为主源；地形对齐《公路路线设计规范》JTG D20 四档 | 3feb1ea |
| 道路等级细化 | 快速路/县乡道细化；高德 road 真实字段兜底；修复交互卡顿（地图分层 + 图表 memo）；路段表 CSV 导出 | 294f951 / bc07c02 / 0d017b9 |
| 数据口径修复 | 修正三处静默算错氢耗的路段数据缺陷 | dadac09 |

## 2026-08-18 · 行为区标注与切分算法

| 工作 | 说明 | 关键提交 |
| --- | --- | --- |
| 氢耗建模知识库 | 30 篇中外文献笔记 + 8 篇全文 + 概念公式（调研资产） | 9561a38 |
| 路段行为区标注 | 变速情况（停止/启动/减速/转弯）+ 变速概率；坡度带自适应切分升级；事件只计实际经过的匝道/收费站 | b47451f / a1dfe36 |
| 切分全面审查 | 同事件区合并成单段 + 事件段典型均速 + 语义统一；城市红绿灯事件 + 概率纯数字 + 表格排序 + 路段↔地图高亮联动 | e6f98d9 / 951ac17 / 584ea67 |

## 2026-08-17 · 调研与细分

| 工作 | 说明 | 关键提交 |
| --- | --- | --- |
| 物理模型调研复核 | 三重锚点（7.1/7.8/8.0 kg/100km）逐条联网核实，L2 准静态选型 | 9c24f49 |
| 路段自适应细分 | 创新点（坡度变号/坡度带阈值细分，短段不合并）+ 路线规划缓存修复 | 999af20 |

## 早期里程碑（08-15 ~ 08-16）

- 路线路况模块（候选路线 + 实时路况 + 加氢站）→ `35a69b1` ~ `a6a89f4`
- 分段切片 + SegmentData 输入契约（物理模型可插拔接口）→ `12cadfe`
- DEM 高程提取 + 路段数据分析面板 + AI 智能评估 → `b9fe95a`
- 高德地理编码精确解析优先 + 内置城市表兜底 → `6493d99`
- 路线选择优先 + 开始测算按钮 + 真实进度条 + 可返回换线 → `14131b2`
- 高德 Key 配置说明 + .env 加载支持 → `668dbdc`

---

## 收尾说明

- 实车 CSV（含 GPS 轨迹）与 `.env` 均被 `.gitignore` 覆盖，不进仓库；模型与工况片段库（无坐标）随仓库提交。
- 敏感信息扫描：仓库内无硬编码 Key/Token（全部环境变量注入）。
